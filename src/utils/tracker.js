// Clean friction-free visitor experience (No user detail or device access code)

export function trackUserEvent(action, details = '') {
  // Completely no-op (Zero tracking or detail collection)
}

export function initAutoTracker() {
  // Completely no-op (Zero device or user detail access)
}
